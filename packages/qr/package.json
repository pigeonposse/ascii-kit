{
	"name": "@ascii-kit/qr",
	"version": "0.0.5",
	"description": "Create QR in ascii art",
	"keywords": [
		"dovenv",
		"pigeonposse",
		"pp"
	],
	"type": "module",
	"exports": {
		".": {
			"import": {
				"types": "./dist/main.d.mts",
				"default": "./dist/main.mjs"
			}
		}
	},
	"files": [
		"dist"
	],
	"scripts": {
		"build": "unbuild",
		"test": "vitest run -r src --passWithNoTests"
	},
	"dependencies": {
		"qrcode-terminal": "0.12.0"
	},
	"devDependencies": {
		"@ascii-kit/repo-config": "workspace:*",
		"@types/qrcode-terminal": "0.12.2"
	},
	"publishConfig": {
		"access": "public",
		"registry": "https://registry.npmjs.org/"
	}
}
